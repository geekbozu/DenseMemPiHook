{
  "name": "dense-mem-hooks",
  "version": "0.1.0",
  "description": "Pi extension: injects Dense-Mem context from previous sessions at session start and adds memory usage instructions to the system prompt.",
  "keywords": ["pi-package", "dense-mem", "memory"],
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test --import ./test/stub-pi.mjs test/config.test.mjs test/tools.test.mjs"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "pi": {
    "extensions": ["./extensions"]
  }
}
